<%= form_with(model: chantier) do |form| %>
  <% if chantier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chantier.errors.count, "error") %> prohibited this chantier from being saved:</h2>

      <ul>
        <% chantier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.collection_select(:client_id, @clients, :id, :nom, {:prompt => true}) %>
  </div>

   <div>
    <%= form.label :date_de_livraison, style: "display: block" %>
    <%= form.date_field :date_de_livraison %>
  </div>

  <div>
    <%= form.label :livre, "Livré ?", style: "display: block" %>
    <%= form.check_box :livre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
