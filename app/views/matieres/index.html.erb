<p style="color: green"><%= notice %></p>

<h1>Matières</h1>

<div class="table" id="matieres">
  <% @matieres.each do |matiere| %>
    <%= render matiere %>
    <div class="table-cell">
      <%= link_to "Détails de la matiere", matiere %>
    </div>
   </div>
  <% end %>
</div>

<%= link_to "Nouvelle matière", new_matiere_path %>