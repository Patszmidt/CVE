<%= form_with(model: matiere) do |form| %>
  <% if matiere.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(matiere.errors.count, "error") %> prohibited this matiere from being saved:</h2>

      <ul>
        <% matiere.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
